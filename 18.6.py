def continue_continue_continue_continue_making_sure_the_function_name_is_correct(s, b):
    x = float(b.split(";")[0])
    y = float(b.split(";")[1])
    k, b = s.split('x')
    k, b = int(k), int(b)
    y2 = k * x + b
    y2 != y
    print(y2 == y)